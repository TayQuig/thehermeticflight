---
const navItems = [
  { href: '/', label: 'Home' },
  { href: '/faq', label: 'FAQ' },
  { href: '/blog', label: 'Flight Log' },
];
---

<header class="fixed top-0 left-0 z-50 w-full border-b border-white/10 bg-black/30 backdrop-blur-md transition-all duration-500">
  <div class="max-w-7xl mx-auto px-6 sm:px-8 lg:px-12 h-20 flex items-center justify-between">
    
    <!-- Logo: Cinzel Bold, Gold -->
    <a href="/" class="text-xl md:text-2xl font-cinzel font-bold tracking-widest text-hermetic-gold hover:text-hermetic-flame transition-colors drop-shadow-[0_0_10px_rgba(197,160,89,0.3)]">
      The Hermetic Flight
    </a>

    <!-- Desktop Nav: Cinzel, Tracking Widest -->
    <nav class="hidden md:block">
      <ul class="flex items-center space-x-10">
        {navItems.map(item => (
          <li>
            <a href={item.href} class="font-cinzel text-sm tracking-widest text-indigo-100 hover:text-hermetic-gold transition-all duration-300 relative group py-2 uppercase">
              {item.label}
              <!-- Gold Underline -->
              <span class="absolute inset-x-0 bottom-0 h-px bg-hermetic-gold scale-x-0 group-hover:scale-x-100 transition-transform duration-500 ease-out"></span>
            </a>
          </li>
        ))}
      </ul>
    </nav>

    <!-- Mobile Menu Button -->
    <div class="md:hidden">
        <a href="#footer-nav" class="text-hermetic-gold font-cinzel text-sm tracking-widest">MENU</a>
    </div>
  </div>
</header>